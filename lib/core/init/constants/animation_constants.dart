import 'package:flutter/animation.dart';

class AnimationConstants {
  //Duration
  static const Duration duration1 = Duration(milliseconds: 10);
  static const Duration duration2 = Duration(milliseconds: 50);
  static const Duration duration3 = Duration(milliseconds: 100);
  static const Duration duration4 = Duration(milliseconds: 300);
  static const Duration duration5 = Duration(milliseconds: 500);
  static const Duration duration10 = Duration(milliseconds: 1000);

  //Animation Duration
  static const Duration walkthroughSliderDuration = Duration(milliseconds: 300);

  //Curves
  static const Curve curve1 = Curves.easeInOutQuart;
}
